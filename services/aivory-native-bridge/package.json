{
  "name": "aivory-native-bridge",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "MCP-to-webhook bridge: exposes tenant-scoped n8n workflows as real MCP servers for Cerveau. Binds tenant identity to the MCP session at connection time (from the connection URL's tenant_id query param), never trusts a tenant_id argument supplied by the calling LLM.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.19.0",
    "pg": "^8.13.1",
    "zod": "^3.23.0"
  }
}
